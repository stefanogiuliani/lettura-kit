# La scheda di catalogo: e' la FONTE della classificazione.
#
# ⚠️ TIER 2 e non 1: le app la pinnano a un TAG, non a `@main`. Un errore non raggiunge
# nessuno finche' qualcuno non alza la versione di proposito. Cio' che e' chiamato a
# `@main` e' tier 1, perche' li' un errore e' vivo per tutti nell'istante del push.
#
# ⚠️ Il nome del repo E' il nome del pacchetto, e non ha prefisso di cliente: serve anche
# a componenti multi-tenant, quindi un prefisso sarebbe falso.
name: lettura-kit
plane: lib
customer: "-"
tier: 2
status: active
problem: "apertura sicura di file tabellari che arrivano da fuori: le guardie contro l'input non fidato, non il dominio di chi legge"
stack: [python]
runtime: "nessuno — e' una libreria: le app la installano via git tag"
owner: stefano.giuliani
depends_on: []
